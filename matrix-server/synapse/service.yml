apiVersion: v1
kind: Service
metadata:
  labels:
    app: synapse
  name: synapse-srv
  namespace: matrix-acc
spec:
  ports:
    - name: http
      protocol: TCP
      port: 8008
    - name: https
      protocol: TCP
      port: 8448
  selector:
    app: synapse
